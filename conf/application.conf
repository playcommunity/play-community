## Secret key
# http://www.playframework.com/documentation/latest/ApplicationSecret
# ~~~~~
# The secret key is used to sign Play's session cookie.
# This must be changed for production, but we don't recommend you change it in this file.
play.http.secret.key = "ADSFSFDSGDDFGSHDSGHFDFGHJDGJH"

play.modules.enabled += "play.modules.reactivemongo.ReactiveMongoModule"

play.http.parser {
  maxMemoryBuffer = 10MB
  maxDiskBuffer = 20MB
}

play.http.forwarded.trustedProxies=["::1", "127.0.0.1"]

mongodb.uri = "mongodb://106.14.154.101:27117/community?authMode=scram-sha1"
mongodb.local.uri = "mongodb://106.14.154.101:27117/local?authMode=scram-sha1"

play.filters.headers {

  # The X-Frame-Options header. If null, the header is not set.
  frameOptions = null

  # The X-XSS-Protection header. If null, the header is not set.
  xssProtection = null

  # The X-Content-Type-Options header. If null, the header is not set.
  contentTypeOptions = null

  # The X-Permitted-Cross-Domain-Policies header. If null, the header is not set.
  permittedCrossDomainPolicies = null

  # The Content-Security-Policy header. If null, the header is not set.
  # contentSecurityPolicy = "default-src 'unsafe-inline'"
  contentSecurityPolicy = null

  # The Referrer-Policy header. If null, the header is not set.
  referrerPolicy = null

  # If true, allow an action to use .withHeaders to replace one or more of the above headers
  allowActionSpecificHeaders = false
}